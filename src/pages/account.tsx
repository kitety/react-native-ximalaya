import { Button, Text, View } from 'react-native';

const Account = () => {
  return (
    <View>
      <Text>Home</Text>
      <Button title='Go to Detail' onPress={() => {}} />
    </View>
  );
};

export default Account;
